# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Used by Docker Compose to inject values into compose.yaml
ALLOWED_ORIGINS=http://localhost:5173
ALLOWED_HOSTS=*
